---
name: 项目瘦身清理
overview: 删除 dist/ 下 6 份旧打包版本和 build/ 缓存，将项目从约 12.4 GB 瘦身到约 4.4 GB，不动任何源码、库存数据和功能。
todos:
  - id: verify-keep
    content: 核对 dist/1.1.5 打包完整、记录 data/ 当前文件数与大小
    status: completed
  - id: delete-old
    content: 删除 6 个旧 dist 版本目录和 build/ 目录
    status: completed
  - id: verify-after
    content: 复核 data/ 与备份完好，统计瘦身后总大小
    status: completed
isProject: false
---

# 项目瘦身清理计划

## 现状

项目共约 12.4 GB，构成如下：

- `dist/` 8.4 GB：7 份 PyInstaller 打包输出，其中 1.1.0/1.1.1/1.1.2 内还各嵌一份无用的旧 data 副本
- `.venv/` 2.0 GB：开发环境（保留）
- `data/` 491 MB：真正的库存数据（保留，不动）
- `data_backup_20260716/` 415 MB：今日备份（按用户要求保留原地）
- `build/` 165 MB：PyInstaller 中间缓存（可删，下次打包自动重建）

已确认 [app_paths.py](app_paths.py) 中 `project_root()` 会从 exe 目录向上找到仓库根，打包版实际读写的是根目录 `data/`，删除旧版本不影响任何库存数据。

## 清理内容

删除以下目录（均已在 `.gitignore` 中，不影响 git）：

- `dist/1.1.0`、`dist/1.1.1`、`dist/1.1.2`、`dist/1.1.3`、`dist/1.1.4`（旧版本）
- `dist/AlexcardInventory`（与 1.1.5 同一次打包的原始输出，内容重复）
- `build/`（打包缓存）

保留：`dist/1.1.5`（最新版，2026/7/16 20:45 打包）、`data/`、`data_backup_20260716/`、`.venv/`、全部源码。

## 执行步骤

1. 删除前先核对 `dist/1.1.5/AlexcardInventory/AlexcardInventory.exe` 存在且完整
2. 逐个删除上述 6 个 dist 子目录和 build/
3. 删除后统计项目总大小，确认降到约 4.4 GB
4. 确认 `data/` 与 `data_backup_20260716/` 完好无损（文件数与大小不变）

预计释放约 8 GB 空间。